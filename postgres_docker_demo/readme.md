#### demo repo for postgres with docker

1. User can run the container in detached mode
    $ docker-compose up -d

2. docker exec -it postgres-db bash

3. psql -U postgres

4. Create exchnage_rate and transaction table

5. run psql_query.py
